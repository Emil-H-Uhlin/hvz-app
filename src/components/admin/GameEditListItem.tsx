import {useState} from "react";

export default function GameEditListItem() {
    const [game, setGame] = useState()

    async function save() {
        // put to API
    }

    function reset() {
        // reset state
    }

    return <>
    </>
}